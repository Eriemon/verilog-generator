{
  "schema_version": 2,
  "category": "generation",
  "commands": [
    {
      "id": "generation.testbench", "category": "generation", "entrypoint": "scripts.python.generation.tb_generator", "subcommand": "", "title": "生成 Verilog testbench scaffold", "summary": "从 RTL 端口生成 testbench 骨架；没有 expectations 或向量时必须以 $fatal 阻止伪 PASS。", "when_to_use": "已有 RTL 但缺少 testbench，且用户需要可继续补全的 Verilog 骨架时。", "aliases": {"zh": ["生成 testbench", "创建 TB 骨架"], "en": ["generate testbench", "create TB scaffold"]}, "parameters": [{"name": "source", "required": true, "description": "输入 RTL"}, {"name": "--output", "required": true, "description": "testbench 输出路径"}], "invocation_templates": ["python -m scripts.python.generation.tb_generator <input.v> --output <tb_output.v>"], "examples": {"valid": ["python -m scripts.python.generation.tb_generator .\\reports\\verilog\\generated\\rtl\\erie_adapter.v --output .\\reports\\verilog\\tb_erie_adapter.v"], "invalid": ["没有期望值的 scaffold 不得打印 PASS"]}, "prerequisites": ["RTL 模块端口可解析"], "outputs": {"format": "verilog", "success_exit_codes": [0], "failure_exit_codes": [1, 2]}, "risk": {"level": "medium", "writes": true, "warning": "会创建显式 testbench 输出文件"}, "boundaries": ["无 expectations 时必须 $fatal", "scaffold 不是仿真通过证据"], "related_command_ids": ["workflow.verify-existing", "workflow.validate"]
    },
    {
      "id": "generation.corpus-analysis", "category": "generation", "entrypoint": "scripts.python.corpus.analyze_verilog_style_corpus", "subcommand": "", "title": "分析 Verilog 风格语料", "summary": "比较 ideal 与 bad corpus 的结构和风格观察，输出统计事实而不生成评分式权威结论。", "when_to_use": "需要维护可读性规则、模板或 corpus 覆盖证据时。", "aliases": {"zh": ["分析 Verilog 语料", "生成 corpus metrics"], "en": ["analyze Verilog corpus", "generate corpus metrics"]}, "parameters": [{"name": "--ideal", "required": true, "description": "理想语料目录"}, {"name": "--bad", "required": true, "description": "负向语料目录"}, {"name": "--out", "required": true, "description": "统计 JSON 输出"}], "invocation_templates": ["python scripts/python/corpus/analyze_verilog_style_corpus.py --ideal <ideal-dir> --bad <bad-dir> --out <metrics.json>"], "examples": {"valid": ["python scripts/python/corpus/analyze_verilog_style_corpus.py --ideal ../../tests/cases/ideal/rtl --bad ../../tests/cases/bad/rtl --out corpus_metrics.json"], "invalid": ["语料统计不得替代逐条 VG 规则门禁"]}, "prerequisites": ["ideal 和 bad corpus 路径存在"], "outputs": {"format": "json", "success_exit_codes": [0], "failure_exit_codes": [1, 2]}, "risk": {"level": "low", "writes": true, "warning": "只写显式统计输出"}, "boundaries": ["输出是观察事实，不是主观评分", "不修改 corpus"], "related_command_ids": ["quality.verilog", "workflow.evaluate"]
    }
  ]
}
