interface:
  display_name: "Readable Verilog Generator"
  short_description: "Create, review, annotate, and validate readable Verilog RTL"
  default_prompt: "Use $readable-verilog-generator to create, write, review, annotate, and validate readable Verilog RTL with Verilog-specific evidence gates."
policy:
  allow_implicit_invocation: true
