interface:
  display_name: "Erie Verilog Generator"
  short_description: "Generate, analyze, verify, refine, compare, debug, and validate Verilog RTL."
  default_prompt: "Use $erie-verilog-generator to generate new RTL, analyze existing RTL, emit design explanations, run verify-repair flows with diagnostics packs, plan controlled refinement or merge assist, compare semantics, and validate synthesizable Verilog designs."
policy:
  allow_implicit_invocation: true
