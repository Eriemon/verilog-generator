interface:
  display_name: "Readable Verilog Generator"
  short_description: "Create governed Verilog RTL with strict specs and WaveDrom"
  default_prompt: "Use $readable-verilog-generator for readable Verilog RTL; require a same-name spec document with WaveDrom interface timing evidence and Verilog quality gates."
policy:
  allow_implicit_invocation: true
